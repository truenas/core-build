MILESTONE = "MASTER"
VERSION = "9.3.2-"+MILESTONE+"-${BUILD_TIMESTAMP}"
SRC_ROOT = "${BE_ROOT}/freenas/src/"
TRAIN = TRAIN or "${PRODUCT}-9.3-Nightlies"
DOWNLOAD_BASEDIR = "/downloads/9.3.2"
IX_INTERNAL_PATH = "/builds/${PRODUCT}/nightlies/9.3.2/"
FREENAS_ROOT = "${BE_ROOT}/freenas"

if exists("${FREENAS_ROOT}"):
    GIT_REVISION = sh("git -C ${FREENAS_ROOT} rev-parse --verify --short HEAD")

NAME = "${PRODUCT}-"+VERSION
